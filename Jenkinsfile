pipeline {
    agent any
    stages {
        stage('Checkout') {
        steps {
                sh 'ls'
                git 'https://github.com/siddhu12980/BlogSpot'
            }
        }
       
    }
}